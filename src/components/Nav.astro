---
import { Assets } from "astro:assets";
---

<nav id="navBar">
    <a href="../"><img class="logo" src="/MainLogo.png"></img></a>
    <div class="links">
        <a href="/about" class="navLink">About</a>
        <a href="/explorations" class="navLink">Case Studies</a>
    </div>
</nav>

<script>

var scrollTimeout

var prevScrollpos = window.pageYOffset;
window.onscroll = function() {
  var currentScrollPos = window.pageYOffset;
  if (prevScrollpos > currentScrollPos) {
    document.getElementById("navBar").style.top = "0";
  } else {
      document.getElementById("navBar").style.top = "-150px";
  }

  // Event buffering here
  clearTimeout(scrollTimeout)
  scrollTimeout = setTimeout(function(){
    prevScrollpos = currentScrollPos;
  },2) // This delay may need adjustment...
}

</script>

<style>

    #navBar{ transition: top 0.5s; }

    nav{
        position: fixed;
        top: 0px;
        display: flex;
        justify-content: flex-start;
        align-items: center;
        width: 100%;
        height: 100px;
        margin: 20px;
        z-index: 1000;
    }
    .links{
        padding: 10px;
        background-color: var(--color-background);
    }
    .navLink{
        margin: 10px;
        padding: 20px;
        font-size: 24px;
        text-decoration: none;
        color: var(--color-text);
    } .navLink:hover{ color: var(--color-accent); }

    .logo{
        height: 80px;
        margin-left: 20px;
        margin-right: 20px;
    }

</style>